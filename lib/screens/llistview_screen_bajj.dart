import 'package:flutter/material.dart';

class LlistviewScreenBajj extends StatelessWidget {
   
  const LlistviewScreenBajj({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('LlistviewScreenBajj'),
      ),
    );
  }
}