import 'package:flutter/material.dart';

class PistasScreenBajj extends StatelessWidget {
   
  const PistasScreenBajj({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('PistasScreenBajj'),
      ),
    );
  }
}